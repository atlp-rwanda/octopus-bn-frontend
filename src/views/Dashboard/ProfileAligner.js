import React from "react";
import classes from '../../styles/profileAligner.module.css';

const ProfileAligner = (props) => {
    return(
    <div className={classes.SvgDiv}>
        <div className={classes.SideIntro}>

        </div>
    </div>

    );
}

export default ProfileAligner;
